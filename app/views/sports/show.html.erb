<p style="color: green"><%= notice %></p>

<%= render @sport %>

<% @sport.teams.each do |team| %>
  <p>
    <%= link_to team.name , team_path(team.id) %>
    </p>
<% end %>

<div>
  <%= link_to "Back to leagues", sports_path %>
</div>